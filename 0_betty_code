#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    char greeting[] = "Hello, Betty!";

    printf("%s\n", greeting);

    return (0);
}
